{
  "name": "strudel-dev-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test:e2e": "npm run build && playwright test",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.1.9"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^22.10.10",
    "typescript": "^5.0.0"
  }
}
